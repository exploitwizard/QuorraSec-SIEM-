flask>=2.3.3
flask-sqlalchemy>=3.0.5
flask-cors>=4.0.0
flask-sock>=0.2.0
flask-limiter>=3.5.0
RestrictedPython>=7.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0
geoip2>=4.7.0
werkzeug>=2.3.7
websocket-client>=1.6.3
scikit-learn>=1.3.0
matplotlib>=3.9.0
pandas>=2.2.0
numpy>=2.0.0

# Cross-platform production WSGI server (works on Windows, Linux, macOS)
waitress>=2.1.2

# Production WSGI + WebSocket server for Linux/Oracle Cloud deployment
gunicorn>=21.2.0
eventlet>=0.35.0

# TOTP 2FA
pyotp>=2.9.0

# Platform-aware data/log directories
platformdirs>=4.0.0

# Prometheus metrics export
prometheus-client>=0.19.0

# Windows service support (only installed on Windows)
pywin32>=306; sys_platform == "win32"

# -----------------------------------------------------------------------
# Courra-Sec v3.0 — New feature dependencies
# -----------------------------------------------------------------------

# Threat intelligence — VirusTotal / OTX / MISP HTTP calls
# (requests already listed above)

# QR code generation for TOTP setup
qrcode[pil]>=7.4.2

# Optional: suppress urllib3 SSL warnings for MISP self-signed certs
urllib3>=2.0.0

# Transactional email (team invitations)
resend>=2.0.0
